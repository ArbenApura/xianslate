node_modules/
/build
/build-capacitor
/.svelte-kit
/package

# CAPACITOR: the android/ native project IS committed (its own android/.gitignore excludes build
# outputs, local.properties, the synced web assets, and capacitor.config.json). iOS is deferred.
/ios
/.capacitor
.env
.env.*
!.env.example
vite.config.ts.timestamp-*

# LOCAL SQLITE DATABASE
*.db
*.db-shm
*.db-wal
*.bak-*
*.recovered-*

# REASONIX DESKTOP TOOLING STATE
/.reasonix

# DEV / TEST ARTIFACTS
# smoke-stability_20260613 verification harnesses (temp, contain test creds) — never commit
scripts/_*.mjs
*.log
*.log.err
sse-test.txt
sse-cache.txt
tools/run-fetch-test.mjs
tools/learned-maps.json
tools/run-cover-test.mjs

# NPM LOCKFILE — PROJECT USES YARN
package-lock.json
coverage
